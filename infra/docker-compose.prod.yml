# Prod override: built images, no source mounts. Backend is the scratch binary
# (Dockerfile.prod); frontend is the vite build served by nginx, which proxies
# /api to the backend (same origin). Only the frontend is published; the
# backend is reachable only inside the network. Merged on docker-compose.yml.
#
# Reproducibility note: the base compose pins image *minor* tags (postgres:18,
# redis:8, …). For a hardened prod deploy, pin them by digest instead
# (e.g. `image: postgres:18@sha256:<digest>`) so a re-pull cannot drift. Resolve
# digests in your release pipeline (`docker buildx imagetools inspect <ref>`).
# An override with no services at all is not a valid compose file; the
# empty mapping keeps `make up` working on an infra-only scaffold.
# feat:if backend|frontend|postgresql|rabbitmq|redis
services:
# feat:else
#~ services: {}
# feat:end
  # feat:if backend
  backend:
    build:
      context: ../services/backend
      dockerfile: .docker/Dockerfile.prod
    environment:
      # The backend is not published here — every request arrives through the
      # frontend's nginx. Without proxy trust, c.IP() is nginx's address for all
      # of them, so the per-IP rate limiter degrades into one shared bucket and
      # a single caller can lock every user out of /auth. nginx overwrites
      # X-Forwarded-For (see services/frontend/.docker/nginx.conf), which is what
      # makes reading it safe.
      MY_PROJECT_API_FIBER_TRUST_PROXY: "true"
      # The compose bridge network. Narrow it if you know the subnet exactly.
      MY_PROJECT_API_FIBER_TRUSTED_PROXIES: "172.16.0.0/12"
    healthcheck:
      # Scratch image has no shell/curl; the prod build ships a /health binary.
      test: ["CMD", "/health"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          memory: 128M

  # feat:end

  # feat:if frontend
  frontend:
    build:
      context: ../services/frontend
      dockerfile: .docker/Dockerfile.prod
    ports:
      - "${MY_PROJECT_FRONTEND_PORT:-8080}:80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/"]
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M

  # feat:end

  # feat:if postgresql
  postgresql:
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          memory: 256M

  # feat:end

  # feat:if rabbitmq
  rabbitmq:
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          memory: 128M

  # feat:end

  # feat:if redis
  redis:
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
        reservations:
          memory: 64M
  # feat:end
